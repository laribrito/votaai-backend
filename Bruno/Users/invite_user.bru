meta {
  name: Invite User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/users/invite/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "first_name": "Jane",
    "last_name": "Doe",
    "username": "janedoe",
    "email": "jane@example.com",
    "roles": ["Redator"]
  }
}
